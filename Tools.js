var primalevil = {
  /**  
  test a contact
    @param {string} name - The contact name.
    @param {Arguments} options - Extra options
      
    @example
    primalevil.test();
    */
    test: function() {
	var temp = "!!!!";
	return temp;
    }
}
